apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: benchmark
    component: workers
  name: benchmark-workers-cadence
spec:
  replicas: 1
  selector:
    matchLabels:
      app: benchmark
      component: workers
  template:
    metadata:
      labels:
        app: benchmark
        component: workers
    spec:
      containers:
      - image: ghcr.io/temporalio/benchmark-workers-cadence:latest
        imagePullPolicy: Always
        name: benchmark-workers
        env:
        - name: CADENCE_GRPC_ENDPOINT
          value: "cadence-frontend.cadence:7833"
        - name: CADENCE_DOMAIN
          value: "default"
        - name: CADENCE_TASK_LIST
          value: "benchmark"
        - name: CADENCE_MAX_DECISION_TASK_POLLERS
          value: "16"
        - name: CADENCE_MAX_ACTIVITY_TASK_POLLERS
          value: "8"
      restartPolicy: Always
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: benchmark
    component: soak-test
  name: benchmark-soak-test-cadence
spec:
  replicas: 1
  selector:
    matchLabels:
      app: benchmark
      component: soak-test
  template:
    metadata:
      labels:
        app: benchmark
        component: soak-test
    spec:
      containers:
      - image: ghcr.io/temporalio/benchmark-workers-cadence:latest
        imagePullPolicy: Always
        name: benchmark-soak-test
        env:
        - name: CADENCE_GRPC_ENDPOINT
          value: "cadence-frontend.cadence:7833"
        - name: CADENCE_DOMAIN
          value: "default"
        - name: CADENCE_TASK_LIST
          value: "benchmark"
        - name: CONCURRENT_WORKFLOWS
          value: "10"
        command: ["runner", "-w", "-c", "$(CONCURRENT_WORKFLOWS)", "-t", "ExecuteActivity", '{ "Count": 3, "Activity": "Echo", "Input": { "Message": "test" } }']
      restartPolicy: Always
